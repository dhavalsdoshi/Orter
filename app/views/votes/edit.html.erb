<h1>Editing vote</h1>

<% form_for([@point, @vote]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @vote %> |
<%= link_to 'Back', point_votes_path(@point) %>
