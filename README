== Set up

1. Make sure your using rubygems 1.8.25

    rvm rubygems 1.8.25 

2. Create config/database.yml file (you can use config.database.yml.sample)

3. Run tests

    bundle exec rake test:units

== Start

bundle exec ruby script/server
